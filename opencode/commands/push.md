---
description: Stage all files, commit with a clean message, and push to remote
---

Stage all changed and untracked files with `git add .`.

Write a short, clean commit message ending with a period. Match the existing commit style in the repo.

Push with `git push origin HEAD` so it works even if the local branch has no upstream remote tracking.

Report the commit hash and branch pushed.
